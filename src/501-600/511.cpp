#include <iostream>
#include <cassert>
#include <vector>
#include <algorithm>
#include <cstdint>
#include <map>
#include <numeric>
#include <functional>

using namespace std;

int const prime_factor[] = {5, 41, 25343, 237631};

int main() {
    // assert(accumulate(prime_factor, prime_factor + 4, 1LL, multiplies<int64_t>()) == 1234567898765LL);
    return 0;
}